language: minimal
arch: arm64

jobs:
  include:
    - name: Integration tests
      install:
        - curl -sfL https://get.k3s.io | sh -s - --write-kubeconfig-mode 644
        - kubectl get nodes
      script:
        - make integration-test
